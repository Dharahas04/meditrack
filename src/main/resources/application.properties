server.port=${SERVER_PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/meditrack_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:meditrack_user}
spring.datasource.password=${DB_PASSWORD:meditrack_password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

jwt.secret=${JWT_SECRET:replace_this_with_real_secret_min_32_chars_1234567890}
jwt.expiration=${JWT_EXPIRATION_MS:86400000}

# Disable public self-registration by default (set true only when needed)
app.auth.public-register-enabled=${PUBLIC_REGISTER_ENABLED:false}

spring.application.name=meditrack-backend
jusyty i need to copy paste it??? or any changes i need to make